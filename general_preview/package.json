{
  "name": "general_preview",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint \"src/**/*.js\" --max-warnings=0",
    "types": "tsc --noEmit",
    "build:test": "esbuild \"src/app/main.js\" --bundle --format=esm --platform=browser --outdir=.check-dist --sourcemap=inline",
    "check": "npm run lint && npm run types && npm run build:test"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "typescript": "^5.5.4",
    "esbuild": "^0.21.5"
  }
}